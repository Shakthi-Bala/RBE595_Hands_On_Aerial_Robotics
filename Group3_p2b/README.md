Steps before running the script

1. Change the absolute path given in simulator.py for splatConfig, renderSettings

Steps to run the script:

1. conda activate vizflyt

2. cd Group3_p2b/

3. python3 main.py /path/to/map.txt
